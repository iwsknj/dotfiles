---
name: tmux-cheatsheet
description: kenj のカスタム tmux キーバインドに関する質問に答えるスキル。tmux の操作方法、キーバインド、ペイン・ウィンドウ・セッション管理について聞かれたときに使う。例：「tmuxでペインを分割するには？」「prefixキーは何？」「ウィンドウを切り替えるキーは？」
---

# tmux チートシート

キーバインド情報は `~/dotfiles/tmux/cheatSheet.md` を読み込んで回答する。

**Prefix キー**: `Ctrl + t`（デフォルトの `Ctrl-b` から変更済み）

質問に答える際は、該当するキーバインドを具体的に示す。
